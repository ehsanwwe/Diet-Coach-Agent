---
phase: 01-infra-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - backend/.gitkeep
  - frontend/.gitkeep
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Repository root contains exactly backend/ and frontend/ directories with no app source files at root level"
    - ".gitignore excludes *.db, *.sqlite, *.sqlite3, all .env files, audio upload dirs, Python caches, and Node caches"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive ignore rules for monorepo"
      contains: "*.sqlite"
    - path: "backend/"
      provides: "Backend application root"
    - path: "frontend/"
      provides: "Frontend application root"
  key_links:
    - from: ".gitignore"
      to: "backend/*.db"
      via: "glob pattern *.db"
      pattern: "\\*\\.db"
---

<objective>
Create the monorepo directory skeleton and `.gitignore` that forms the foundation for all subsequent work.

Purpose: Every other plan depends on this directory layout existing. The `.gitignore` must be correct from day 1 — SQLite files, env files, and audio uploads must never be committed.
Output: `backend/` and `frontend/` directories at repo root, a comprehensive `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo directory skeleton</name>
  <files>backend/.gitkeep, frontend/.gitkeep</files>
  <read_first>
    - .gitignore (if it exists — check before overwriting)
  </read_first>
  <action>
Create these two directories at the repository root by placing a `.gitkeep` file in each. This satisfies INFRA-01: monorepo contains exactly `backend/` and `frontend/` — no app source files live at root level.

The repo root already contains: CLAUDE.md, DECISIONS.md, CHANGELOG.md, NEXT_STEPS.md, PROJECT_STATE.md, PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, config.json, LICENSE — these are planning/docs files, not app source. That is correct.

Create:
- `backend/.gitkeep` — empty file, just marks the directory
- `frontend/.gitkeep` — empty file, just marks the directory

Do NOT create any subdirectories inside backend/ or frontend/ yet — that is the job of later plans.
  </action>
  <verify>
    <automated>Test-Path "D:\ai_agent\Diet-Coach-Agent\backend" -PathType Container; Test-Path "D:\ai_agent\Diet-Coach-Agent\frontend" -PathType Container</automated>
  </verify>
  <acceptance_criteria>
    - `backend/` directory exists at repo root
    - `frontend/` directory exists at repo root
    - No Python source files exist at repo root (only docs/planning files)
  </acceptance_criteria>
  <done>Both directories exist; no app source at root.</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (read current content first if it exists)
  </read_first>
  <action>
Write `.gitignore` at the repository root. The file must cover all categories below. Per INFRA-02, it MUST exclude SQLite files, env files, audio/upload dirs, caches, and `.planning/` internal dirs.

Write EXACTLY this content to `.gitignore`:

```
# ============================================================
# Diet Coach Agent — .gitignore
# ============================================================

# ── SQLite database files (NEVER commit) ────────────────────
*.db
*.sqlite
*.sqlite3

# ── Environment files (NEVER commit secrets) ────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
backend/.env
frontend/.env
# .env.example files ARE committed — they document required vars

# ── Audio / upload directories ───────────────────────────────
backend/uploads/
backend/audio/
backend/media/

# ── Python caches and build artifacts ───────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.Python
env/
venv/
.venv/
ENV/
.env.bak
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.pytest_cache/
htmlcov/

# ── uv (Python package manager) ─────────────────────────────
.uv/

# ── Node / Next.js ───────────────────────────────────────────
node_modules/
.next/
out/
.turbo/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm

# ── TypeScript build output ──────────────────────────────────
*.tsbuildinfo
next-env.d.ts

# ── OS artifacts ─────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ── IDE / editor ─────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── GSD planning internal dirs (not for end users) ──────────
.planning/codebase/
.planning/research/

# ── Alembic SQLite artifact ──────────────────────────────────
backend/app.db
```

This satisfies INFRA-02 completely.
  </action>
  <verify>
    <automated>Select-String -Path "D:\ai_agent\Diet-Coach-Agent\.gitignore" -Pattern "\*\.sqlite" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\.gitignore" -Pattern "\.env$" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\.gitignore" -Pattern "uploads/" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `*.sqlite` (grep: `\*.sqlite`)
    - `.gitignore` contains `*.db` (grep: `\*.db`)
    - `.gitignore` contains `.env` bare line (grep: `^\.env$`)
    - `.gitignore` contains `backend/uploads/` (grep: `backend/uploads/`)
    - `.gitignore` contains `__pycache__/` (grep: `__pycache__/`)
    - `.gitignore` contains `node_modules/` (grep: `node_modules/`)
    - `.gitignore` contains `.next/` (grep: `\.next/`)
    - `.gitignore` does NOT contain `.env.example` as an ignored pattern (those must be committed)
  </acceptance_criteria>
  <done>`.gitignore` is committed and covers all required categories from INFRA-02.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls` repo root shows `backend/` and `frontend/` directories
2. `cat .gitignore | grep "*.sqlite"` returns a match
3. `cat .gitignore | grep ".env.example"` returns NO match (example files must be committed)
</verification>

<success_criteria>
- `backend/` and `frontend/` directories exist at repo root (INFRA-01)
- `.gitignore` excludes: `*.db`, `*.sqlite`, `*.sqlite3`, `.env`, `backend/.env`, `frontend/.env`, `backend/uploads/`, `__pycache__/`, `node_modules/`, `.next/` (INFRA-02)
- `.env.example` files are NOT ignored (they must be committed to document vars)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-backend-foundation/01-01-SUMMARY.md`
</output>
