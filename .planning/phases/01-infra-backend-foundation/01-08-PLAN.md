---
phase: 01-infra-backend-foundation
plan: 08
type: execute
wave: 7
depends_on:
  - 01-06
  - 01-07
files_modified:
  - backend/.env.example
  - frontend/.env.example
  - .env.example
  - backend/README.md
  - frontend/README.md
  - PROJECT_STATE.md
  - NEXT_STEPS.md
  - DECISIONS.md
  - CHANGELOG.md
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-07
  - INFRA-08
  - INFRA-09
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "backend/.env.example lists all 10 OPENCLAW_* variables with descriptions"
    - "backend/.env.example lists SECRET_KEY with generation instructions"
    - "frontend/.env.example documents all required frontend env vars"
    - "Root .env.example documents cross-service vars and links to backend/frontend examples"
    - "backend/README.md documents setup, migration commands, and env vars"
    - "frontend/README.md documents setup and development commands"
    - "PROJECT_STATE.md reflects Phase 1 completion state"
    - "NEXT_STEPS.md describes exactly what to do next (Phase 2)"
    - "CHANGELOG.md has an entry for Phase 1 completion"
    - "DECISIONS.md has any new decisions from Phase 1 appended"
  artifacts:
    - path: "backend/.env.example"
      provides: "Backend environment variable documentation with all 10 OPENCLAW_* vars"
      contains: "OPENCLAW_BASE_URL"
    - path: "frontend/.env.example"
      provides: "Frontend environment variable documentation"
      contains: "NEXT_PUBLIC"
    - path: ".env.example"
      provides: "Root cross-service env documentation"
      contains: "backend/.env.example"
    - path: "backend/README.md"
      provides: "Backend setup guide"
      contains: "alembic upgrade head"
    - path: "PROJECT_STATE.md"
      provides: "Current state for cold-start resumption"
      contains: "Phase 1"
    - path: "CHANGELOG.md"
      provides: "Phase 1 completion entry"
      contains: "Phase 1"
  key_links:
    - from: ".env.example"
      to: "backend/.env.example"
      via: "cross-reference link"
      pattern: "backend/.env.example"
    - from: "backend/.env.example"
      to: "OPENCLAW_BASE_URL"
      via: "documented env var"
      pattern: "OPENCLAW_BASE_URL"
---

<objective>
Write all env example files, README files, and update the four continuation files. This is the final plan of Phase 1.

Purpose: Env example files document all required variables (especially all 10 OPENCLAW_* vars) so any new developer can set up the project. Continuation files ensure cold-start resumption works after Phase 1.
Output: 3 `.env.example` files, 2 README files, and updated PROJECT_STATE.md, NEXT_STEPS.md, DECISIONS.md, CHANGELOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write all three .env.example files and two README files</name>
  <files>
    backend/.env.example,
    frontend/.env.example,
    .env.example,
    backend/README.md,
    frontend/README.md
  </files>
  <read_first>
    - backend/app/core/config.py (extract all Settings fields — these all need documentation)
    - CLAUDE.md (INFRA-03: all 10 OPENCLAW_* vars must be documented in backend/.env.example)
  </read_first>
  <action>
Create five files. Write EXACTLY the content specified.

---

**`backend/.env.example`** — Backend environment variables (INFRA-03):

```dotenv
# ============================================================
# Diet Coach Agent — Backend Environment Variables
# ============================================================
# Copy this file to backend/.env and fill in values.
# NEVER commit backend/.env to git.
# ============================================================

# ── App ──────────────────────────────────────────────────────
APP_NAME="Diet Coach Agent"
APP_VERSION="0.1.0"
# Set to "production" in production; affects debug output
ENVIRONMENT=development
DEBUG=false

# ── Security (REQUIRED) ──────────────────────────────────────
# REQUIRED: Server will refuse to start without this.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# JWT token expiry in minutes (default: 7 days = 10080 minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ── Database ─────────────────────────────────────────────────
# SQLite path relative to backend/ directory.
# Run: alembic upgrade head (from backend/) to initialize.
DATABASE_URL=sqlite+pysqlite:///./app.db

# ── CORS Origins ─────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# Development: http://localhost:3000
# Production: https://your-deployed-frontend.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ── Development OTP ──────────────────────────────────────────
# OTP code accepted in development mode (bypasses SMS).
# Set to a random value or remove in production.
DEV_OTP_CODE=123456

# ── OpenClaw AI Provider ─────────────────────────────────────
# All 10 OPENCLAW_* variables. Required for real AI responses.
# When OPENCLAW_BASE_URL is not set, app uses MockAIProvider (no error).
#
# OPENCLAW_BASE_URL
#   The base URL of the OpenAI-compatible AI endpoint.
#   Example: https://api.openai.com  or  https://your-openclaw-endpoint.com
#   Leave empty to use MockAIProvider (deterministic Persian responses).
OPENCLAW_BASE_URL=

# OPENCLAW_API_KEY
#   API key for authenticating requests to the AI endpoint.
#   Sent as: Authorization: Bearer <key>
#   Leave empty if endpoint does not require auth (e.g., local LLM).
OPENCLAW_API_KEY=

# OPENCLAW_MODEL
#   Model name sent in the request body.
#   Example: gpt-4o, gpt-3.5-turbo, llama3, mistral
OPENCLAW_MODEL=gpt-4o

# OPENCLAW_CHAT_COMPLETIONS_PATH
#   Path appended to OPENCLAW_BASE_URL for chat completions.
#   OpenAI-compatible: /v1/chat/completions
OPENCLAW_CHAT_COMPLETIONS_PATH=/v1/chat/completions

# OPENCLAW_TIMEOUT_SECONDS
#   HTTP request timeout in seconds. Requests exceeding this are cancelled.
#   Increase for slow models; decrease for fast local LLMs.
OPENCLAW_TIMEOUT_SECONDS=30

# OPENCLAW_MAX_RETRIES
#   Number of retry attempts on transient network errors (429, 5xx).
#   Set to 0 to disable retries.
OPENCLAW_MAX_RETRIES=3

# OPENCLAW_TEMPERATURE
#   Sampling temperature for AI responses. 0.0=deterministic, 1.0=creative.
#   Nutrition advice: 0.6-0.8 recommended for varied but grounded responses.
OPENCLAW_TEMPERATURE=0.7

# OPENCLAW_MAX_TOKENS
#   Maximum tokens in each AI response.
#   Increase for longer meal plans; decrease to reduce cost.
OPENCLAW_MAX_TOKENS=1024

# OPENCLAW_CONTEXT_MAX_MESSAGES
#   Maximum number of conversation messages sent in each AI request.
#   When exceeded, oldest messages are pruned (context window management).
#   Increase for richer context; decrease to reduce token cost.
OPENCLAW_CONTEXT_MAX_MESSAGES=20

# OPENCLAW_CONTEXT_SUMMARY_ENABLED
#   When true: generates a rolling summary when OPENCLAW_CONTEXT_MAX_MESSAGES
#   is reached, stores it in ChatSession.summary, and injects it into the
#   next session's system prompt prefix.
#   When false: oldest messages are pruned without summarization.
OPENCLAW_CONTEXT_SUMMARY_ENABLED=false
```

---

**`frontend/.env.example`** — Frontend environment variables (INFRA-04):

```dotenv
# ============================================================
# Diet Coach Agent — Frontend Environment Variables
# ============================================================
# Copy this file to frontend/.env.local and fill in values.
# NEVER commit frontend/.env.local to git.
# Variables starting with NEXT_PUBLIC_ are exposed to the browser.
# ============================================================

# ── API Configuration ─────────────────────────────────────────
# URL of the backend API. Used by Next.js for server-side fetches.
# In development: http://localhost:8000
# In production: https://your-backend.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── App Configuration ─────────────────────────────────────────
# Default locale if none is detected from cookie or Accept-Language.
# One of: fa (Persian/RTL), en (English/LTR), ar (Arabic/RTL)
NEXT_PUBLIC_DEFAULT_LOCALE=fa
```

---

**`.env.example`** — Root cross-service env documentation (INFRA-09):

```dotenv
# ============================================================
# Diet Coach Agent — Root Environment Variables
# ============================================================
# This file documents cross-service environment variables.
#
# For service-specific variables, see:
#   backend/.env.example   — Backend (FastAPI) variables
#   frontend/.env.example  — Frontend (Next.js) variables
#
# Copy this file to .env at repository root for any shared
# orchestration tools (docker-compose, CI, etc.).
# ============================================================

# ── Development Ports ─────────────────────────────────────────
# Backend API server port (uvicorn)
BACKEND_PORT=8000

# Frontend dev server port (Next.js)
FRONTEND_PORT=3000

# ── Cross-Service ─────────────────────────────────────────────
# Used if running both services via a shared script or docker-compose.
# Individual service .env files take precedence for service-specific vars.

# Backend API URL (referenced by frontend when building)
NEXT_PUBLIC_API_URL=http://localhost:8000
```

---

**`backend/README.md`** — Backend setup guide (INFRA-07):

```markdown
# Diet Coach Agent — Backend

FastAPI + SQLAlchemy 2.x + Alembic + SQLite backend.

## Requirements

- Python 3.12+
- [uv](https://docs.astral.sh/uv/) (recommended) or pip

## Setup

### 1. Copy environment file

```bash
cp .env.example .env
```

Edit `.env` and set:
- `SECRET_KEY` — generate with: `python -c "import secrets; print(secrets.token_hex(32))"`
- `OPENCLAW_BASE_URL` — leave empty to use MockAIProvider (no AI key required for development)

### 2. Install dependencies

With uv (recommended):
```bash
uv sync
```

With pip:
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -e ".[dev]"
```

### 3. Initialize the database

```bash
alembic upgrade head
```

This creates `app.db` (SQLite) with all 22 tables.

### 4. Start the development server

```bash
uvicorn app.main:app --reload
```

Server starts on http://localhost:8000  
OpenAPI docs: http://localhost:8000/docs

## Database Migrations

| Command | Description |
|---------|-------------|
| `alembic upgrade head` | Apply all pending migrations |
| `alembic downgrade base` | Roll back all migrations (drops all tables) |
| `alembic downgrade -1` | Roll back one migration |
| `alembic revision --autogenerate -m "description"` | Create new migration from model changes |
| `alembic history` | View migration history |

### SQLite Safeguards

`alembic/env.py` has two mandatory flags:
- `render_as_batch=True` — required for `ALTER TABLE` operations on SQLite
- `compare_type=True` — detects column type changes during autogenerate

**Never remove these flags.**

## Environment Variables

See `backend/.env.example` for all variables with descriptions.

Key variables:
- `SECRET_KEY` — **Required.** App refuses to start without it.
- `OPENCLAW_BASE_URL` — Optional. Leave empty for MockAIProvider.
- `DATABASE_URL` — Defaults to `sqlite+pysqlite:///./app.db`
- `CORS_ORIGINS` — Comma-separated frontend origins

## Project Structure

```
backend/
├── app/
│   ├── api/v1/          # Route handlers (endpoints)
│   ├── core/            # Config, database, errors
│   ├── models/          # SQLAlchemy ORM models
│   ├── schemas/         # Pydantic v2 request/response schemas
│   ├── services/        # Business logic layer
│   └── repositories/    # Data access layer
├── alembic/             # Database migrations
│   └── versions/        # Migration files
├── alembic.ini          # Alembic config
└── pyproject.toml       # Python project manifest
```

## Technology Notes

- **SQLAlchemy 2.x ONLY**: Use `select()` + `session.execute()`. Never `session.query()`.
- **lazy="raise"**: All ORM relationships raise an error if accessed outside a session.
- **PyJWT** (not python-jose): `jwt.encode()` / `jwt.decode()` with HS256.
- **pydantic-settings**: All config via `Settings(BaseSettings)` in `app/core/config.py`.
```

---

**`frontend/README.md`** — Frontend setup guide (INFRA-08):

```markdown
# Diet Coach Agent — Frontend

Next.js 16 + TypeScript + Tailwind CSS v4 frontend.

## Requirements

- Node.js 20.9+
- npm (included with Node.js)

## Setup

### 1. Install dependencies

```bash
npm install
```

### 2. Copy environment file

```bash
cp .env.example .env.local
```

Edit `.env.local` and set `NEXT_PUBLIC_API_URL` to your backend URL.

### 3. Start the development server

```bash
npm run dev
```

App starts on http://localhost:3000  
Navigate to http://localhost:3000/fa (Persian) or http://localhost:3000/en (English).

## Scripts

| Script | Description |
|--------|-------------|
| `npm run dev` | Start development server with Turbopack |
| `npm run build` | Build for production |
| `npm run start` | Start production server |
| `npm run lint` | Run ESLint |
| `npm run type-check` | TypeScript type check (no emit) |

## Technology Notes

- **Tailwind CSS v4**: CSS logical properties by default. Use `ps-/pe-/ms-/me-`, never `pl-/pr-/ml-/mr-`.
- **App Router**: All routes under `src/app/[lang]/` for i18n support.
- **TypeScript strict**: `"strict": true` in tsconfig.json — all type errors must be fixed.
- **No `any`**: Use strict types; RTL direction bugs are caught by the type system.
```
  </action>
  <verify>
    <automated>Select-String -Path "D:\ai_agent\Diet-Coach-Agent\backend\.env.example" -Pattern "OPENCLAW_BASE_URL" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\backend\.env.example" -Pattern "OPENCLAW_CONTEXT_SUMMARY_ENABLED" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\backend\README.md" -Pattern "alembic upgrade head" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\.env.example" -Pattern "backend/.env.example" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `backend/.env.example` contains all 10 OPENCLAW_* vars (grep each: OPENCLAW_BASE_URL, OPENCLAW_API_KEY, OPENCLAW_MODEL, OPENCLAW_CHAT_COMPLETIONS_PATH, OPENCLAW_TIMEOUT_SECONDS, OPENCLAW_MAX_RETRIES, OPENCLAW_TEMPERATURE, OPENCLAW_MAX_TOKENS, OPENCLAW_CONTEXT_MAX_MESSAGES, OPENCLAW_CONTEXT_SUMMARY_ENABLED)
    - `backend/.env.example` contains `SECRET_KEY=` with generation instructions (grep: `SECRET_KEY`)
    - `frontend/.env.example` contains `NEXT_PUBLIC_API_URL` (grep: `NEXT_PUBLIC_API_URL`)
    - Root `.env.example` references `backend/.env.example` and `frontend/.env.example` (grep: `backend/.env.example`)
    - `backend/README.md` contains `alembic upgrade head` (grep: `alembic upgrade head`)
    - `backend/README.md` contains `render_as_batch` warning (grep: `render_as_batch`)
    - `frontend/README.md` contains `npm run dev` (grep: `npm run dev`)
  </acceptance_criteria>
  <done>All three .env.example files written. Both README files written with setup instructions, migration commands, and technology notes.</done>
</task>

<task type="auto">
  <name>Task 2: Update continuation files for Phase 1 completion</name>
  <files>
    PROJECT_STATE.md,
    NEXT_STEPS.md,
    DECISIONS.md,
    CHANGELOG.md
  </files>
  <read_first>
    - PROJECT_STATE.md (read current content before overwriting)
    - NEXT_STEPS.md (read current content before overwriting)
    - DECISIONS.md (read current content — APPEND ONLY, never delete)
    - CHANGELOG.md (read current content — APPEND ONLY)
    - .planning/ROADMAP.md (read Phase 2 goal to write accurate NEXT_STEPS)
  </read_first>
  <action>
Update all four continuation files to reflect Phase 1 completion. These files enable cold-start resumption after `/clear`.

**CRITICAL RULES:**
- `DECISIONS.md` is APPEND-ONLY — read existing content first, then append new entries. NEVER delete existing entries.
- `CHANGELOG.md` is APPEND-ONLY — read existing content first, prepend or append new entry at top under `## [Unreleased]` or create `## [0.1.0] - 2026-06-03`.
- `PROJECT_STATE.md` and `NEXT_STEPS.md` are REPLACED with current state.

---

**`PROJECT_STATE.md`** — Replace entirely with current state:

```markdown
# Project State — Diet Coach Agent

**Last updated:** 2026-06-03  
**Current phase:** Phase 1 COMPLETE → Phase 2 next  
**Overall progress:** Phase 1 of 10 complete (10%)

## What Exists Now

### Backend (`backend/`)
- `pyproject.toml` — all dependencies declared (FastAPI, SQLAlchemy 2.x, Alembic, PyJWT, pydantic-settings)
- `app/core/config.py` — pydantic-settings BaseSettings, SECRET_KEY validation, all 10 OPENCLAW_* vars
- `app/core/database.py` — SQLAlchemy 2.x sync engine, get_session(), DeclarativeBase
- `app/core/errors.py` — consistent error response helpers (AppError, error_response, raise_http_error)
- `app/models/` — ALL 22 ORM models defined with lazy="raise" on all relationships
  - Group 1: User, AuthOTP, TokenBlocklist, AuditLog, UserLanguagePreference
  - Group 2: UserProfile, MedicalCondition, UserMedicalFlag, Medication, Allergy
  - Group 3: LifestyleProfile, FoodPreference, BehaviorProfile
  - Group 4: NutritionGoal, NutritionRiskAssessment, NutritionPlan, NutritionPlanMeal
  - Group 5: ChatSession, ChatMessage, AudioMessage, MealEntry
  - Group 6: DailyCheckIn, ProgressEntry, WeeklyReport
- `alembic/` — Alembic configured with render_as_batch=True + compare_type=True
- `alembic/versions/0001_initial_schema.py` — initial migration for all 22 tables
- `app/main.py` — FastAPI app factory, CORS from env, global error handlers
- `app/api/v1/router.py` — v1 router, health endpoint at /api/v1/health
- `app/schemas/common.py` — ErrorResponse, SuccessResponse, PaginatedResponse (Pydantic v2)
- `backend/.env.example` — all env vars documented including all 10 OPENCLAW_* vars

### Frontend (`frontend/`)
- Next.js 16 skeleton with App Router `src/app/[lang]/` structure
- Tailwind CSS v4 with `@tailwindcss/postcss` PostCSS plugin
- TypeScript strict mode enabled
- `frontend/.env.example` — frontend env vars documented

### Root
- `.gitignore` — SQLite, env files, uploads, caches all excluded
- `.env.example` — cross-service env vars, links to service examples
- `backend/README.md`, `frontend/README.md` — setup guides

## Critical Safeguards Verified
- `lazy="raise"` on ALL ORM relationships (no silent N+1 possible)
- `render_as_batch=True` in BOTH alembic/env.py context.configure() calls
- `SECRET_KEY` validation at startup — server refuses to start without it
- No hard-coded CORS origins — loaded from `CORS_ORIGINS` env var

## Known Issues / Blockers
- Phase 7 AI Layer: Persian food nutrition accuracy needs validation
- Phase 6 Voice: iOS Safari MediaRecorder behavior needs real-device testing

## How to Resume (Cold Start)
1. Read this file + NEXT_STEPS.md
2. `cd backend && SECRET_KEY=... alembic upgrade head` to verify DB
3. `cd backend && SECRET_KEY=... uvicorn app.main:app` to verify server
4. Start Phase 2: `/gsd:plan-phase 2` or `/gsd:execute-phase 2`
```

---

**`NEXT_STEPS.md`** — Replace entirely with Phase 2 next steps:

```markdown
# Next Steps — Diet Coach Agent

**Updated:** 2026-06-03  
**Current status:** Phase 1 COMPLETE. Phase 2 ready to start.

## Immediate Next Action

**Start Phase 2: i18n & Frontend Shell**

Command: `/gsd:plan-phase 2` (then `/gsd:execute-phase 2`)

## What Phase 2 Builds

Phase 2 goal: Complete i18n foundation — fa/en/ar dictionaries, RTL direction from cookie (zero flicker), all CSS using Tailwind v4 logical properties, direction-aware utilities, PWA manifest + service worker, muted/pale/app-like style system.

### First files to touch in Phase 2:
1. `frontend/src/middleware.ts` — locale detection from cookie/Accept-Language, redirect to /[lang]
2. `frontend/src/app/[lang]/dictionaries/fa.json` — Persian dictionary (all UI strings)
3. `frontend/src/app/[lang]/dictionaries/en.json` — English dictionary
4. `frontend/src/app/[lang]/dictionaries/ar.json` — Arabic dictionary
5. `frontend/src/app/[lang]/layout.tsx` — add `lang` and `dir` attributes from server-side locale

### First command to run:
```bash
cd frontend && npm install  # if not already done
# Then execute Phase 2 plans
```

## If Phase 2 is Already Done

Read `.planning/phases/02-*/02-*-SUMMARY.md` files to understand what was built.  
Then check `ROADMAP.md` for Phase 3 (Authentication).

## Phase Order Reminder

1. ✅ Infra & Backend Foundation (COMPLETE)
2. → i18n & Frontend Shell (NEXT)
3. Authentication (after Phase 2)
4. Onboarding Backend (after Phase 3)
5. Onboarding Frontend (after Phase 4)
6. Voice & Audio (after Phase 5)
7. Nutrition Backend & AI Layer (after Phase 4)
8. Nutrition Frontend & Chat (after Phase 7)
9. Progress & Reports (after Phase 8)
10. Settings, Polish & Remaining UI (after Phase 9)
```

---

**`DECISIONS.md`** — APPEND to existing content (read current content first, then append):

Append the following section at the END of the existing file, after the last entry:

```markdown

---

## 2026-06-03 — Phase 1 Implementation Decisions

### D-014: All ORM models use String(36) for UUID primary keys (not native UUID type)
**Decision:** Use `String(36)` with `default=lambda: str(uuid.uuid4())` for all primary keys.
**Rationale:** SQLite has no native UUID type. String(36) is portable to PostgreSQL without migration changes. The Alembic render_as_batch=True flag handles future type changes safely.
**Alternatives rejected:** Python UUID type (dialect-specific, requires extra Alembic config); Integer PKs (prevents distributed ID generation).

### D-015: JSON list fields stored as Text (not JSON type)
**Decision:** Fields containing lists (disliked_foods, cravings, report_data, etc.) use `Text` column type, with application-layer JSON serialization.
**Rationale:** SQLite has no native JSON type (JSON1 extension varies); using Text is portable to PostgreSQL (which does have JSON). The application layer owns serialization/deserialization.
**Alternatives rejected:** SQLAlchemy JSON type (uses SQLite's JSON1 extension — not universally available); separate junction tables (over-engineered for list fields that are read/written as units).

### D-016: SECRET_KEY validated via pydantic field_validator at import time
**Decision:** `settings = Settings()` is called at module level in `config.py`; the `@field_validator("SECRET_KEY")` runs immediately, raising `ValueError` if absent or placeholder. FastAPI import of `settings` in `main.py` triggers this at startup.
**Rationale:** Fails fast — the process never starts with a missing key. No runtime check needed in routes.
**Alternatives rejected:** Lifespan event validation (later failure, harder to catch in tests); manual check in each route (error-prone, could be missed).
```

---

**`CHANGELOG.md`** — APPEND Phase 1 entry at the top under Unreleased (read current content first):

Prepend the following BEFORE existing content (or after `## [Unreleased]` header if it exists):

```markdown
## [0.1.0] - 2026-06-03

### Added — Phase 1: Infra & Backend Foundation

**Backend**
- Monorepo structure: `backend/` and `frontend/` directories established
- `backend/pyproject.toml` with all required dependencies (FastAPI, SQLAlchemy 2.x, Alembic, PyJWT, pydantic-settings)
- `backend/app/core/config.py`: pydantic-settings BaseSettings with SECRET_KEY validation and all 10 OPENCLAW_* vars
- `backend/app/core/database.py`: SQLAlchemy 2.x sync engine, `get_session()` FastAPI dependency, `DeclarativeBase`
- `backend/app/core/errors.py`: consistent `{"status", "message", "detail"}` error response helpers
- All 22 ORM models with `lazy="raise"` on all relationships, UUID PKs, created_at/updated_at
- Alembic migration environment with `render_as_batch=True` + `compare_type=True` (SQLite DDL safety)
- Initial migration `0001_initial_schema.py` covering all 22 tables
- FastAPI app factory with CORS from environment, global error handlers, modular directory structure
- `/api/v1/health` endpoint returning `{"status": "ok"}`
- `backend/.env.example` documenting all env vars including all 10 OPENCLAW_* vars
- `backend/README.md` with setup, migration commands, technology notes

**Frontend**
- Next.js 16 skeleton with App Router `src/app/[lang]/` structure
- Tailwind CSS v4 with `@tailwindcss/postcss` PostCSS plugin
- TypeScript strict mode enabled
- `frontend/.env.example` documenting frontend env vars
- `frontend/README.md` with setup guide

**Repository**
- `.gitignore` covering SQLite files, env files, uploads, caches
- Root `.env.example` with cross-service documentation linking to service examples

### Notes
- `lazy="raise"` on all ORM relationships: accessing any relationship outside a session raises MissingGreenlet/DetachedInstanceError
- `render_as_batch=True` in BOTH alembic/env.py context.configure() calls — required for all future SQLite DDL
- SECRET_KEY absent causes server to refuse startup with clear error message
```
  </action>
  <verify>
    <automated>Select-String -Path "D:\ai_agent\Diet-Coach-Agent\PROJECT_STATE.md" -Pattern "Phase 1" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\NEXT_STEPS.md" -Pattern "Phase 2" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\DECISIONS.md" -Pattern "D-014" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\CHANGELOG.md" -Pattern "Phase 1" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `PROJECT_STATE.md` contains "Phase 1" and lists all 22 models (grep: `Phase 1`)
    - `PROJECT_STATE.md` mentions `lazy="raise"` and `render_as_batch=True` as verified safeguards
    - `NEXT_STEPS.md` contains "Phase 2" and describes what to build next (grep: `Phase 2`)
    - `NEXT_STEPS.md` mentions `middleware.ts` as the first file to create in Phase 2
    - `DECISIONS.md` contains D-014, D-015, D-016 entries (grep: `D-014`)
    - `DECISIONS.md` still contains D-001 through D-013 (existing content not deleted) (grep: `D-001`)
    - `CHANGELOG.md` contains a `[0.1.0]` or Phase 1 entry (grep: `Phase 1`)
    - `CHANGELOG.md` mentions `render_as_batch=True` in the Phase 1 notes
  </acceptance_criteria>
  <done>All four continuation files updated. PROJECT_STATE.md reflects Phase 1 completion. NEXT_STEPS.md describes Phase 2 start. DECISIONS.md has D-014/015/016 appended. CHANGELOG.md has Phase 1 entry.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `backend/.env.example` contains all 10 OPENCLAW_* vars (grep each)
2. `backend/README.md` contains `alembic upgrade head` and `render_as_batch`
3. `PROJECT_STATE.md` mentions lazy="raise" and render_as_batch=True as verified
4. `DECISIONS.md` still starts with D-001 (existing content preserved)
5. `CHANGELOG.md` has Phase 1 entry with `[0.1.0]`
</verification>

<success_criteria>
- `backend/.env.example` documents all 10 OPENCLAW_* variables with descriptions (INFRA-03)
- `frontend/.env.example` documents frontend env vars (INFRA-04)
- Root `.env.example` links to backend and frontend examples (INFRA-09)
- `backend/README.md` documents alembic commands and render_as_batch warning (INFRA-07)
- `frontend/README.md` documents npm commands (INFRA-08)
- `PROJECT_STATE.md` reflects Phase 1 complete state (CONT-01)
- `NEXT_STEPS.md` describes exact Phase 2 start actions (CONT-02)
- `DECISIONS.md` has D-014/015/016 appended without deleting existing entries (CONT-03)
- `CHANGELOG.md` has Phase 1 entry (CONT-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-backend-foundation/01-08-SUMMARY.md`
</output>
