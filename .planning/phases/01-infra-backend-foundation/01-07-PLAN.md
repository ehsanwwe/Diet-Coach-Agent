---
phase: 01-infra-backend-foundation
plan: 07
type: execute
wave: 6
depends_on:
  - 01-01
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/postcss.config.mjs
  - frontend/next.config.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/[lang]/layout.tsx
  - frontend/src/app/[lang]/page.tsx
  - frontend/src/app/globals.css
autonomous: true
requirements:
  - INFRA-06
  - INFRA-08

must_haves:
  truths:
    - "npm run dev from frontend/ starts the Next.js 16 dev server on port 3000"
    - "App Router structure uses frontend/src/app/[lang]/ layout from the start"
    - "Tailwind CSS v4 configured with @tailwindcss/postcss PostCSS plugin"
    - "TypeScript strict mode enabled"
    - "No hard-coded text in any component (skeleton only — no features)"
  artifacts:
    - path: "frontend/package.json"
      provides: "Next.js 16 project manifest"
      contains: "next"
    - path: "frontend/postcss.config.mjs"
      provides: "PostCSS config with @tailwindcss/postcss plugin"
      contains: "@tailwindcss/postcss"
    - path: "frontend/src/app/[lang]/layout.tsx"
      provides: "Root layout for [lang] route segment"
      contains: "lang"
    - path: "frontend/tsconfig.json"
      provides: "TypeScript config with strict mode"
      contains: "strict"
  key_links:
    - from: "frontend/src/app/[lang]/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "import from layout"
      pattern: "globals.css"
    - from: "frontend/postcss.config.mjs"
      to: "@tailwindcss/postcss"
      via: "PostCSS plugin"
      pattern: "@tailwindcss/postcss"
---

<objective>
Bootstrap the Next.js 16 frontend skeleton with the App Router `[lang]` layout structure, Tailwind CSS v4, and TypeScript strict mode. This is a skeleton only — no features, no i18n dictionaries (those are Phase 2).

Purpose: Establishes the correct directory structure (especially `src/app/[lang]/`) from day 1. Adding i18n or RTL to an LTR-first app is prohibitively expensive. Phase 2 builds on this layout.
Output: A runnable Next.js 16 dev server with skeleton layout files and Tailwind v4 PostCSS configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@CLAUDE.md
@DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project files (package.json, tsconfig, postcss, next.config)</name>
  <files>
    frontend/package.json,
    frontend/tsconfig.json,
    frontend/postcss.config.mjs,
    frontend/next.config.ts
  </files>
  <read_first>
    - CLAUDE.md (D-005: Tailwind v4, @tailwindcss/postcss, Next.js 16 App Router)
    - DECISIONS.md (D-003 Persian/RTL-first, D-005 Tailwind v4)
  </read_first>
  <action>
Create four configuration files for the frontend. Do NOT run `create-next-app` — write files manually to avoid prompts and ensure exact configuration.

---

**`frontend/package.json`** — Next.js 16 + React 19 + Tailwind v4:
```json
{
  "name": "diet-coach-agent-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
```

NOTES:
- `next dev --turbopack` — Turbopack is the default in Next.js 16
- `tailwindcss: ^4.0.0` with `@tailwindcss/postcss: ^4.0.0` — v4 requires this PostCSS plugin (D-005)
- React 19 ships with Next.js 16 (never install React 18 separately)
- `type-check` script: run `tsc --noEmit` to check types separately from build

---

**`frontend/tsconfig.json`** — TypeScript 5.x strict mode:
```json
{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

NOTE: `"strict": true` is MANDATORY — catches RTL direction bugs and locale type mismatches (CLAUDE.md constraint).

---

**`frontend/postcss.config.mjs`** — Tailwind v4 PostCSS plugin (D-005):
```js
/** @type {import('postcss').Config} */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

CRITICAL: Tailwind v4 uses `@tailwindcss/postcss` — NOT the v3 `tailwindcss` plugin. Using the wrong plugin will silently produce no CSS.

---

**`frontend/next.config.ts`** — Minimal Next.js config:
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Strict mode for development
  reactStrictMode: true,
};

export default nextConfig;
```
  </action>
  <verify>
    <automated>Select-String -Path "D:\ai_agent\Diet-Coach-Agent\frontend\package.json" -Pattern '"next"' -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\frontend\postcss.config.mjs" -Pattern "@tailwindcss/postcss" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\frontend\tsconfig.json" -Pattern '"strict": true' -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"next": "^16.0.0"` (grep: `"next"`)
    - `frontend/package.json` contains `"tailwindcss": "^4.0.0"` (grep: `tailwindcss`)
    - `frontend/package.json` contains `"@tailwindcss/postcss": "^4.0.0"` (grep: `@tailwindcss/postcss`)
    - `frontend/package.json` does NOT contain `"tailwind.config.js"` references (Tailwind v4 CSS-first)
    - `frontend/postcss.config.mjs` uses `@tailwindcss/postcss` as the plugin key (grep: `@tailwindcss/postcss`)
    - `frontend/tsconfig.json` has `"strict": true` (grep: `"strict": true`)
    - `frontend/tsconfig.json` has `"@/*": ["./src/*"]` path alias (grep: `@/\*`)
    - `frontend/next.config.ts` is TypeScript (not .js or .mjs) and exports `NextConfig` type
  </acceptance_criteria>
  <done>Next.js 16 project config files written. Tailwind v4 with @tailwindcss/postcss plugin, TypeScript strict mode enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Create App Router skeleton with [lang] layout structure</name>
  <files>
    frontend/src/app/layout.tsx,
    frontend/src/app/globals.css,
    frontend/src/app/[lang]/layout.tsx,
    frontend/src/app/[lang]/page.tsx
  </files>
  <read_first>
    - CLAUDE.md (Next.js 16 App Router structure requirement: frontend/src/app/[lang]/)
    - DECISIONS.md (D-003 Persian RTL-first, D-005 Tailwind v4 logical properties)
  </read_first>
  <action>
Create the App Router skeleton. This is Phase 1 skeleton only — no dictionaries, no i18n middleware, no RTL logic yet (all Phase 2). The [lang] route segment is established NOW so Phase 2 never needs to restructure.

---

**`frontend/src/app/globals.css`** — Tailwind v4 CSS entry (no tailwind.config.js needed):
```css
@import "tailwindcss";

/*
  Diet Coach Agent — Global Styles
  
  Tailwind v4: CSS-first configuration via @theme {} block.
  No tailwind.config.js required.
  
  Key constraints:
  - Use logical properties: ps-/pe-/ms-/me- NEVER pl-/pr-/ml-/mr-
  - Use Tailwind v4 logical property utilities throughout
  - CSS custom properties defined in @theme {} are available as Tailwind utilities
  
  Phase 2 will add: color palette, font stack, RTL-specific overrides
*/

@theme {
  /* Font stack — updated in Phase 2 with Persian font */
  --font-family-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  /* Direction set server-side via [lang] layout in Phase 2 */
  font-family: var(--font-family-sans);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  margin: 0;
  padding: 0;
  min-height: 100dvh;
}
```

---

**`frontend/src/app/layout.tsx`** — Root layout (redirects to [lang] — minimal shell):
```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Diet Coach Agent",
  description: "Your AI-powered nutrition companion",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    // Phase 2 will: set lang and dir attributes server-side from cookie/param
    // For now: skeleton only, direction set in [lang]/layout.tsx
    <html>
      <body>{children}</body>
    </html>
  );
}
```

---

**`frontend/src/app/[lang]/layout.tsx`** — Locale-specific layout (Phase 2 will add RTL logic here):
```tsx
/**
 * [lang] Route Segment Layout
 *
 * This layout wraps all language-specific routes.
 * Phase 2 will add:
 *   - Server-side locale validation (fa | en | ar)
 *   - dir="rtl" | dir="ltr" on the html element
 *   - Dictionary loading via getDictionary(lang)
 *   - Cookie-based locale persistence
 *
 * For Phase 1: skeleton only. Renders children directly.
 */
import type { Metadata } from "next";

type Props = {
  children: React.ReactNode;
  params: Promise<{ lang: string }>;
};

export async function generateStaticParams() {
  // Phase 2 will expand this to support all locales.
  // Returning empty array for now — dynamic rendering in dev.
  return [];
}

export const metadata: Metadata = {
  title: "Diet Coach Agent",
};

export default async function LangLayout({ children, params }: Props) {
  const { lang } = await params;

  return (
    // Phase 2 replaces this with: lang={validatedLang} dir={lang === "en" ? "ltr" : "rtl"}
    <div data-lang={lang}>
      {children}
    </div>
  );
}
```

---

**`frontend/src/app/[lang]/page.tsx`** — Skeleton home page (no features):
```tsx
/**
 * Skeleton home page for [lang] route.
 *
 * Phase 1: Renders a plain "coming soon" shell with no features.
 * Phase 2: Replaced with proper i18n-aware landing page.
 * Phase 3: Replaced with auth-gated home or redirect to login.
 */
type Props = {
  params: Promise<{ lang: string }>;
};

export default async function HomePage({ params }: Props) {
  const { lang } = await params;

  return (
    <main className="min-h-dvh flex items-center justify-center">
      <div className="text-center p-8">
        <h1 className="text-2xl font-semibold mb-4">Diet Coach Agent</h1>
        <p className="text-sm text-gray-500">
          Phase 1 skeleton — lang: {lang}
        </p>
      </div>
    </main>
  );
}
```

After writing all files, install dependencies and verify dev server starts:

```bash
cd frontend
npm install
npm run dev &
sleep 5
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/fa
# Expected: 200
kill %1
```

If `npm install` fails due to network issues, ensure the package.json is written correctly and retry. The lock file (package-lock.json) will be generated by npm install and should be committed.
  </action>
  <verify>
    <automated>Select-String -Path "D:\ai_agent\Diet-Coach-Agent\frontend\src\app\[lang]\layout.tsx" -Pattern "lang" -Quiet; Select-String -Path "D:\ai_agent\Diet-Coach-Agent\frontend\src\app\globals.css" -Pattern "@import.*tailwindcss" -Quiet; Test-Path "D:\ai_agent\Diet-Coach-Agent\frontend\src\app\[lang]\page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/app/[lang]/layout.tsx` exists and uses `params: Promise<{ lang: string }>` (grep: `lang`)
    - `frontend/src/app/[lang]/page.tsx` exists
    - `frontend/src/app/globals.css` starts with `@import "tailwindcss"` (grep: `@import.*tailwindcss`)
    - `frontend/src/app/globals.css` contains `@theme {` block (Tailwind v4 CSS-first config) (grep: `@theme`)
    - No Tailwind physical property classes used in any file: `pl-`, `pr-`, `ml-`, `mr-` are absent (grep must return NO matches)
    - `frontend/src/app/layout.tsx` imports globals.css (grep: `globals.css`)
    - `frontend/node_modules/` exists after `npm install` (dev server can start)
    - `npm run dev` from `frontend/` starts without immediate errors (INFRA-06)
  </acceptance_criteria>
  <done>Next.js 16 App Router skeleton with [lang] layout structure. Tailwind v4 CSS entry point established. No physical CSS properties used.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `frontend/src/app/[lang]/layout.tsx` and `[lang]/page.tsx` exist
2. `frontend/postcss.config.mjs` uses `@tailwindcss/postcss` (grep)
3. `frontend/tsconfig.json` has `"strict": true`
4. `npm run dev` from `frontend/` starts Next.js on port 3000
5. `curl http://localhost:3000/fa` returns 200
</verification>

<success_criteria>
- `npm run dev` from `frontend/` starts the Next.js 16 dev server (INFRA-06)
- App Router directory structure uses `src/app/[lang]/` from day 1
- Tailwind v4 configured with `@tailwindcss/postcss` PostCSS plugin (D-005)
- TypeScript strict mode enabled in tsconfig.json
- No physical CSS properties (pl-, pr-, ml-, mr-) used in any file
- `frontend/README.md` creation deferred to Plan 08 (env/docs wave)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra-backend-foundation/01-07-SUMMARY.md`
</output>
