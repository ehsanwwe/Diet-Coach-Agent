---
phase: 09-progress-reports
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/progress.ts
  - frontend/src/lib/progress.ts
  - frontend/src/dictionaries/fa.ts
  - frontend/src/dictionaries/en.ts
  - frontend/src/dictionaries/ar.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
must_haves:
  truths:
    - "Frontend has typed contracts for all three Phase 9 endpoints — Plan 03 components can import them directly with no `any` types"
    - "Frontend has API client functions submitCheckIn, getProgressSummary, getWeeklyReport that include the Bearer token, handle 401, and surface server error messages"
    - "Dictionary interface in fa.ts has a `progress` namespace with all 49 keys defined; en.ts and ar.ts implement the same keys with translated copy"
    - "TypeScript type-check passes with no errors (`tsc --noEmit`)"
  artifacts:
    - path: "frontend/src/types/progress.ts"
      provides: "TypeScript types mirroring backend Pydantic schemas"
      exports: ["CheckInRequest", "CheckInResponse", "BehaviorWin", "WeightTrend", "ProgressSummaryResponse", "WeeklyReportData", "WeeklyReportResponse"]
    - path: "frontend/src/lib/progress.ts"
      provides: "API client for /api/v1/progress/* endpoints"
      exports: ["submitCheckIn", "getProgressSummary", "getWeeklyReport"]
    - path: "frontend/src/dictionaries/fa.ts"
      provides: "Dictionary interface + Persian translations for progress namespace"
      contains: "progress:"
    - path: "frontend/src/dictionaries/en.ts"
      provides: "English translations for progress namespace"
      contains: "progress:"
    - path: "frontend/src/dictionaries/ar.ts"
      provides: "Arabic translations for progress namespace"
      contains: "progress:"
  key_links:
    - from: "frontend/src/lib/progress.ts"
      to: "frontend/src/types/progress.ts"
      via: "import type"
      pattern: "from '@/types/progress'"
    - from: "frontend/src/lib/progress.ts"
      to: "frontend/src/lib/storage.ts"
      via: "import { getToken }"
      pattern: "from './storage'"
    - from: "frontend/src/dictionaries/en.ts"
      to: "frontend/src/dictionaries/fa.ts"
      via: "implements Dictionary interface"
      pattern: "const en: Dictionary"
---

<objective>
Define the frontend contract for Phase 9: TypeScript types mirroring the backend Pydantic schemas (Plan 01), the API client functions for the three progress endpoints, and the `progress` dictionary namespace in fa/en/ar (49 keys each, per UI-SPEC).

Purpose: Establish the typed boundary that Plan 03 (components and page) consumes — components import these types and call these functions; no `any`, no inline string literals, no extra fetch wrappers.

Output: 2 new files, 3 modified dictionary files. `tsc --noEmit` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-progress-reports/09-RESEARCH.md
@.planning/phases/09-progress-reports/09-UI-SPEC.md

<interfaces>
<!-- Existing project patterns the executor must follow exactly. -->

From frontend/src/lib/storage.ts (use these — DO NOT reimplement):
```typescript
export function getToken(): string | null
export function setToken(token: string): void
export function clearToken(): void
```

From frontend/src/lib/nutrition.ts (mirror this EXACT pattern in lib/progress.ts):
```typescript
import { getToken } from './storage'
import type { ... } from '@/types/nutrition'

const BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? 'http://localhost:8000'
const BASE = '/api/v1/nutrition'

function authHeaders(): Record<string, string> {
  const token = getToken()
  return token ? { Authorization: `Bearer ${token}` } : {}
}

async function handleResponse<T>(res: Response): Promise<T> {
  if (res.status === 401) throw new Error('UNAUTHORIZED')
  if (!res.ok) {
    const err = await res.json().catch(() => ({ message: 'Request failed' }))
    throw new Error(err?.detail?.message ?? err?.message ?? 'Request failed')
  }
  return res.json() as Promise<T>
}

export async function getNutritionProfile(): Promise<NutritionProfileResponse> {
  const res = await fetch(`${BASE_URL}${BASE}/profile`, { headers: { ...authHeaders() } })
  return handleResponse<NutritionProfileResponse>(res)
}
```

From frontend/src/dictionaries/fa.ts (Dictionary interface — append a new `progress:` block at the end of the interface body, before the closing brace at line 452, then implement the matching object at the end of `const fa: Dictionary = { ... }` at line 895):
```typescript
export interface Dictionary {
  audio: { ... }
  common: { ... }
  splash: { ... }
  nav: { home, chat, progress, settings }
  language: { ... }
  errors: { ... }
  auth: { ... }
  onboarding: { ... }
  ...
  safety: { ... }
  // <-- ADD progress: { ... } HERE, after safety
}
```

From .planning/phases/09-progress-reports/09-UI-SPEC.md (Copywriting Contract — canonical Persian copy for every key)

Backend response schemas (Plan 01 — `backend/app/schemas/progress.py` — mirror EXACTLY in TypeScript):
- CheckInRequest: { check_date: date (YYYY-MM-DD string), weight_kg?: float 20..300, hunger_level?: int 1..5, sleep_hours?: float 0..24, stress_level?: int 1..5, activity_minutes?: int 0..1440, adherence_notes?: string }
- CheckInResponse: { id: string, check_date: string (date), weight_kg|null, hunger_level|null, sleep_hours|null, stress_level|null, activity_minutes|null, adherence_notes|null, created_at: string (datetime) }
- WeightTrend: { first: number, last: number, delta: number }
- BehaviorWin: { key: string, label_key: string, achieved: boolean, value: string|null, tracked: boolean }
- ProgressSummaryResponse: { has_data: boolean, recent_checkins: CheckInResponse[], weight_series: number[], latest_weight_kg: number|null, weight_trend: WeightTrend|null, behavior_wins: BehaviorWin[], logging_streak: number, empty_state_message: string|null }
- WeeklyReportData: { weight_trend: WeightTrend|null, weight_series: number[], avg_hunger: number|null, avg_sleep: number|null, avg_stress: number|null, total_activity_minutes: number, logging_days: number, adherence_pct: number, sleep_stress_note: string|null, suggested_focus: string }
- WeeklyReportResponse: { has_report: boolean, week_start: string|null, week_end: string|null, report: WeeklyReportData|null, empty_state_message: string|null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend types and API client (types/progress.ts + lib/progress.ts)</name>
  <files>frontend/src/types/progress.ts, frontend/src/lib/progress.ts</files>
  <read_first>
    - frontend/src/types/nutrition.ts (reference for type shape, optional fields convention, exact `interface` style)
    - frontend/src/lib/nutrition.ts (reference for API client BASE_URL constant, authHeaders helper, handleResponse helper, fetch wrapper signature)
    - frontend/src/lib/storage.ts (getToken signature)
    - .planning/phases/09-progress-reports/09-RESEARCH.md (Pattern 7 — exact lib/progress.ts contents)
  </read_first>
  <action>
    Create exactly these two files.

    ## File 1: `frontend/src/types/progress.ts` (NEW)
    ```typescript
    /**
     * Progress & Reports types — mirrors backend/app/schemas/progress.py.
     * PROG-01, PROG-02, PROG-03.
     */

    export interface CheckInRequest {
      check_date: string // ISO-8601 date (YYYY-MM-DD)
      weight_kg?: number | null
      hunger_level?: number | null
      sleep_hours?: number | null
      stress_level?: number | null
      activity_minutes?: number | null
      adherence_notes?: string | null
    }

    export interface CheckInResponse {
      id: string
      check_date: string
      weight_kg: number | null
      hunger_level: number | null
      sleep_hours: number | null
      stress_level: number | null
      activity_minutes: number | null
      adherence_notes: string | null
      created_at: string
    }

    export interface WeightTrend {
      first: number
      last: number
      delta: number
    }

    export type BehaviorWinKey =
      | 'sleep'
      | 'activity'
      | 'logging'
      | 'low_stress'
      | 'low_hunger'
      | 'protein'
      | 'fiber'
      | 'hydration'

    export interface BehaviorWin {
      key: BehaviorWinKey
      label_key: string
      achieved: boolean
      value: string | null
      tracked: boolean
    }

    export interface ProgressSummaryResponse {
      has_data: boolean
      recent_checkins: CheckInResponse[]
      weight_series: number[]
      latest_weight_kg: number | null
      weight_trend: WeightTrend | null
      behavior_wins: BehaviorWin[]
      logging_streak: number
      empty_state_message: string | null
    }

    export interface WeeklyReportData {
      weight_trend: WeightTrend | null
      weight_series: number[]
      avg_hunger: number | null
      avg_sleep: number | null
      avg_stress: number | null
      total_activity_minutes: number
      logging_days: number
      adherence_pct: number
      sleep_stress_note: string | null
      suggested_focus: string
    }

    export interface WeeklyReportResponse {
      has_report: boolean
      week_start: string | null
      week_end: string | null
      report: WeeklyReportData | null
      empty_state_message: string | null
    }
    ```

    ## File 2: `frontend/src/lib/progress.ts` (NEW)
    ```typescript
    import { getToken } from './storage'
    import type {
      CheckInRequest,
      CheckInResponse,
      ProgressSummaryResponse,
      WeeklyReportResponse,
    } from '@/types/progress'

    const BASE_URL = process.env.NEXT_PUBLIC_API_BASE_URL ?? 'http://localhost:8000'
    const BASE = '/api/v1/progress'

    function authHeaders(): Record<string, string> {
      const token = getToken()
      return token ? { Authorization: `Bearer ${token}` } : {}
    }

    async function handleResponse<T>(res: Response): Promise<T> {
      if (res.status === 401) {
        throw new Error('UNAUTHORIZED')
      }
      if (!res.ok) {
        const err = await res.json().catch(() => ({ message: 'Request failed' }))
        throw new Error(err?.detail?.message ?? err?.message ?? 'Request failed')
      }
      return res.json() as Promise<T>
    }

    export async function submitCheckIn(body: CheckInRequest): Promise<CheckInResponse> {
      const res = await fetch(`${BASE_URL}${BASE}/check-in`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', ...authHeaders() },
        body: JSON.stringify(body),
      })
      return handleResponse<CheckInResponse>(res)
    }

    export async function getProgressSummary(): Promise<ProgressSummaryResponse> {
      const res = await fetch(`${BASE_URL}${BASE}/summary`, {
        headers: { ...authHeaders() },
      })
      return handleResponse<ProgressSummaryResponse>(res)
    }

    export async function getWeeklyReport(): Promise<WeeklyReportResponse> {
      const res = await fetch(`${BASE_URL}${BASE}/weekly-report`, {
        headers: { ...authHeaders() },
      })
      return handleResponse<WeeklyReportResponse>(res)
    }
    ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/progress.ts` exists and contains the literal strings `export interface CheckInRequest`, `export interface CheckInResponse`, `export interface ProgressSummaryResponse`, `export interface WeeklyReportResponse`, `export interface WeightTrend`, `export interface BehaviorWin`, `export type BehaviorWinKey`
    - `frontend/src/lib/progress.ts` exists and contains `export async function submitCheckIn`, `export async function getProgressSummary`, `export async function getWeeklyReport`
    - `frontend/src/lib/progress.ts` contains the literal string `'/api/v1/progress'`
    - `frontend/src/lib/progress.ts` contains `if (res.status === 401)` (401 short-circuit)
    - `cd frontend && npx tsc --noEmit` exits 0 with no errors
    - `grep -n "any" frontend/src/types/progress.ts frontend/src/lib/progress.ts` returns no matches (no `any` type usage)
  </acceptance_criteria>
  <done>
    Frontend has typed contracts and API client for all three progress endpoints. TypeScript strict-mode compile clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the `progress` namespace to fa/en/ar dictionaries (49 keys per locale)</name>
  <files>frontend/src/dictionaries/fa.ts, frontend/src/dictionaries/en.ts, frontend/src/dictionaries/ar.ts</files>
  <read_first>
    - frontend/src/dictionaries/fa.ts (current Dictionary interface ends at line ~452; current fa object ends at line ~895)
    - frontend/src/dictionaries/en.ts (current en object ends at line ~436)
    - frontend/src/dictionaries/ar.ts (current ar object ends at line ~436)
    - .planning/phases/09-progress-reports/09-UI-SPEC.md (Copywriting Contract — Persian canonical copy for every key)
  </read_first>
  <action>
    Edit all three dictionary files. The Persian copy is canonical (from UI-SPEC); English and Arabic translations must match semantically.

    ## Step A: Modify `frontend/src/dictionaries/fa.ts`

    1) Inside the `export interface Dictionary { ... }` block, AFTER the existing `safety: { ... }` block and BEFORE the closing `}` of the interface, insert a new namespace declaration:

    ```typescript
      progress: {
        // Page header
        title: string
        subtitle: string
        // Tab labels
        tabSummary: string
        tabWeekly: string
        // Check-in form
        checkInTitle: string
        checkInSubtitle: string
        checkInWeight: string
        checkInWeightUnit: string
        checkInHunger: string
        checkInSleep: string
        checkInSleepUnit: string
        checkInStress: string
        checkInActivity: string
        checkInActivityUnit: string
        checkInNotes: string
        checkInNotesPlaceholder: string
        checkInSubmit: string
        checkInSubmitting: string
        checkInSuccess: string
        checkInAlreadyToday: string
        checkInTodayMissing: string
        // Summary screen
        summaryTitle: string
        latestWeight: string
        weightTrend: string
        behaviourWinsTitle: string
        loggingStreak: string
        loggingStreakDays: string
        // Behavior win labels
        winSleep: string
        winActivity: string
        winLogging: string
        winLowStress: string
        winLowHunger: string
        winHydration: string
        winProtein: string
        winFiber: string
        winNotTracked: string
        // Weekly report
        weeklyTitle: string
        weeklyPeriod: string
        weeklyAdherence: string
        weeklyAvgSleep: string
        weeklyAvgStress: string
        weeklyAvgHunger: string
        weeklyTotalActivity: string
        weeklyWeightDelta: string
        weeklySleepStressNote: string
        weeklyFocusTitle: string
        weeklyLoggingDays: string
        // Empty states
        emptyTitle: string
        emptyDesc: string
        emptyCheckinCta: string
        emptyWeeklyTitle: string
        emptyWeeklyDesc: string
        // Errors
        errSubmitFailed: string
        errLoadFailed: string
        // Units
        unitKg: string
        unitHours: string
        unitMinutes: string
        unitPercent: string
      }
    ```

    2) Inside the `const fa: Dictionary = { ... }` object literal, AFTER the existing `safety: { ... },` block and BEFORE the closing `}`, insert the matching Persian implementation:

    ```typescript
      progress: {
        title: 'پیشرفت',
        subtitle: 'مسیر سلامتی شما',
        tabSummary: 'خلاصه',
        tabWeekly: 'گزارش هفتگی',
        checkInTitle: 'ثبت وضعیت امروز',
        checkInSubtitle: 'چند دقیقه وقت بگذارید — اطلاعات بیشتر، راهنمایی بهتر',
        checkInWeight: 'وزن امروز',
        checkInWeightUnit: 'کیلوگرم',
        checkInHunger: 'سطح گرسنگی (۱–۵)',
        checkInSleep: 'ساعت خواب',
        checkInSleepUnit: 'ساعت',
        checkInStress: 'سطح استرس (۱–۵)',
        checkInActivity: 'دقیقه تحرک',
        checkInActivityUnit: 'دقیقه',
        checkInNotes: 'یادداشت پیروی',
        checkInNotesPlaceholder: 'مثلاً: امروز رژیم را رعایت کردم، یک شام سنگین داشتم...',
        checkInSubmit: 'ثبت وضعیت',
        checkInSubmitting: 'در حال ثبت...',
        checkInSuccess: 'وضعیت امروز ثبت شد',
        checkInAlreadyToday: 'امروز ثبت شده — می‌توانید ویرایش کنید',
        checkInTodayMissing: 'امروز ثبت نشده — ثبت وضعیت',
        summaryTitle: 'خلاصه پیشرفت',
        latestWeight: 'آخرین وزن',
        weightTrend: 'روند وزن',
        behaviourWinsTitle: 'دستاوردهای این هفته',
        loggingStreak: 'روزهای متوالی ثبت',
        loggingStreakDays: 'روز',
        winSleep: 'خواب منظم',
        winActivity: 'تحرک کافی',
        winLogging: 'ثبت منظم',
        winLowStress: 'استرس پایین',
        winLowHunger: 'گرسنگی کنترل‌شده',
        winHydration: 'آب کافی',
        winProtein: 'پروتئین کافی',
        winFiber: 'فیبر کافی',
        winNotTracked: 'هنوز ثبت نمی‌شود',
        weeklyTitle: 'گزارش هفتگی',
        weeklyPeriod: 'دوره گزارش',
        weeklyAdherence: 'درصد پیروی',
        weeklyAvgSleep: 'میانگین خواب',
        weeklyAvgStress: 'میانگین استرس',
        weeklyAvgHunger: 'میانگین گرسنگی',
        weeklyTotalActivity: 'کل تحرک',
        weeklyWeightDelta: 'تغییر وزن',
        weeklySleepStressNote: 'نکته خواب و استرس',
        weeklyFocusTitle: 'پیشنهاد هفته بعد',
        weeklyLoggingDays: 'روزهای ثبت‌شده',
        emptyTitle: 'هنوز هیچ ثبتی ندارید',
        emptyDesc: 'اولین ثبت وضعیت را همین‌جا انجام دهید — فقط چند دقیقه کافی است',
        emptyCheckinCta: 'ثبت وضعیت اول',
        emptyWeeklyTitle: 'هنوز داده کافی ندارید',
        emptyWeeklyDesc: 'پس از ۳ روز ثبت، گزارش هفتگی آماده می‌شود',
        errSubmitFailed: 'ثبت ناموفق بود — دوباره تلاش کنید',
        errLoadFailed: 'بارگذاری داده‌ها با مشکل مواجه شد — دوباره تلاش کنید',
        unitKg: 'کیلوگرم',
        unitHours: 'ساعت',
        unitMinutes: 'دقیقه',
        unitPercent: 'درصد',
      },
    ```

    ## Step B: Modify `frontend/src/dictionaries/en.ts`

    Inside the `const en: Dictionary = { ... }` object literal, AFTER the existing `safety: { ... },` block and BEFORE the closing `}`, insert:

    ```typescript
      progress: {
        title: 'Progress',
        subtitle: 'Your wellness journey',
        tabSummary: 'Summary',
        tabWeekly: 'Weekly Report',
        checkInTitle: 'Log Today',
        checkInSubtitle: 'Take a moment — more data means better guidance',
        checkInWeight: "Today's weight",
        checkInWeightUnit: 'kg',
        checkInHunger: 'Hunger level (1–5)',
        checkInSleep: 'Hours of sleep',
        checkInSleepUnit: 'hours',
        checkInStress: 'Stress level (1–5)',
        checkInActivity: 'Active minutes',
        checkInActivityUnit: 'min',
        checkInNotes: 'Adherence notes',
        checkInNotesPlaceholder: 'e.g. Stuck to the plan, had a heavy dinner...',
        checkInSubmit: 'Save Check-in',
        checkInSubmitting: 'Saving...',
        checkInSuccess: "Today's check-in saved",
        checkInAlreadyToday: 'Already logged today — you can edit',
        checkInTodayMissing: 'Not logged today — log now',
        summaryTitle: 'Progress Summary',
        latestWeight: 'Latest weight',
        weightTrend: 'Weight trend',
        behaviourWinsTitle: 'Wins this week',
        loggingStreak: 'Logging streak',
        loggingStreakDays: 'days',
        winSleep: 'Consistent sleep',
        winActivity: 'Active enough',
        winLogging: 'Regular logging',
        winLowStress: 'Low stress',
        winLowHunger: 'Hunger managed',
        winHydration: 'Hydrated',
        winProtein: 'Enough protein',
        winFiber: 'Enough fiber',
        winNotTracked: 'not tracked yet',
        weeklyTitle: 'Weekly Report',
        weeklyPeriod: 'Report period',
        weeklyAdherence: 'Adherence',
        weeklyAvgSleep: 'Avg sleep',
        weeklyAvgStress: 'Avg stress',
        weeklyAvgHunger: 'Avg hunger',
        weeklyTotalActivity: 'Total activity',
        weeklyWeightDelta: 'Weight change',
        weeklySleepStressNote: 'Sleep & stress note',
        weeklyFocusTitle: 'Focus for next week',
        weeklyLoggingDays: 'Days logged',
        emptyTitle: 'No entries yet',
        emptyDesc: 'Log your first check-in here — just a couple of minutes',
        emptyCheckinCta: 'Log first check-in',
        emptyWeeklyTitle: 'Not enough data yet',
        emptyWeeklyDesc: 'Your weekly report will be ready after 3 days of check-ins',
        errSubmitFailed: 'Could not save — please try again',
        errLoadFailed: 'Could not load data — please try again',
        unitKg: 'kg',
        unitHours: 'hours',
        unitMinutes: 'min',
        unitPercent: '%',
      },
    ```

    ## Step C: Modify `frontend/src/dictionaries/ar.ts`

    Inside the `const ar: Dictionary = { ... }` object literal, AFTER the existing `safety: { ... },` block and BEFORE the closing `}`, insert:

    ```typescript
      progress: {
        title: 'التقدم',
        subtitle: 'رحلتك الصحية',
        tabSummary: 'الملخص',
        tabWeekly: 'التقرير الأسبوعي',
        checkInTitle: 'تسجيل اليوم',
        checkInSubtitle: 'خصص بضع دقائق — بيانات أكثر تعني توجيهاً أفضل',
        checkInWeight: 'وزن اليوم',
        checkInWeightUnit: 'كجم',
        checkInHunger: 'مستوى الجوع (١–٥)',
        checkInSleep: 'ساعات النوم',
        checkInSleepUnit: 'ساعة',
        checkInStress: 'مستوى التوتر (١–٥)',
        checkInActivity: 'دقائق النشاط',
        checkInActivityUnit: 'دقيقة',
        checkInNotes: 'ملاحظات الالتزام',
        checkInNotesPlaceholder: 'مثلاً: التزمت بالخطة، تناولت عشاءً ثقيلاً...',
        checkInSubmit: 'حفظ التسجيل',
        checkInSubmitting: 'جارٍ الحفظ...',
        checkInSuccess: 'تم حفظ تسجيل اليوم',
        checkInAlreadyToday: 'تم التسجيل اليوم — يمكنك التعديل',
        checkInTodayMissing: 'لم يتم التسجيل اليوم — سجّل الآن',
        summaryTitle: 'ملخص التقدم',
        latestWeight: 'آخر وزن',
        weightTrend: 'اتجاه الوزن',
        behaviourWinsTitle: 'إنجازات هذا الأسبوع',
        loggingStreak: 'أيام تسجيل متتالية',
        loggingStreakDays: 'يوم',
        winSleep: 'نوم منتظم',
        winActivity: 'نشاط كافٍ',
        winLogging: 'تسجيل منتظم',
        winLowStress: 'توتر منخفض',
        winLowHunger: 'الجوع مُدار',
        winHydration: 'ترطيب كافٍ',
        winProtein: 'بروتين كافٍ',
        winFiber: 'ألياف كافية',
        winNotTracked: 'لم يُسجَّل بعد',
        weeklyTitle: 'التقرير الأسبوعي',
        weeklyPeriod: 'فترة التقرير',
        weeklyAdherence: 'نسبة الالتزام',
        weeklyAvgSleep: 'متوسط النوم',
        weeklyAvgStress: 'متوسط التوتر',
        weeklyAvgHunger: 'متوسط الجوع',
        weeklyTotalActivity: 'إجمالي النشاط',
        weeklyWeightDelta: 'تغيُّر الوزن',
        weeklySleepStressNote: 'ملاحظة النوم والتوتر',
        weeklyFocusTitle: 'تركيز الأسبوع القادم',
        weeklyLoggingDays: 'أيام التسجيل',
        emptyTitle: 'لا توجد تسجيلات بعد',
        emptyDesc: 'سجّل أول تسجيل لك هنا — دقائق قليلة فقط',
        emptyCheckinCta: 'سجّل أول تسجيل',
        emptyWeeklyTitle: 'لا توجد بيانات كافية بعد',
        emptyWeeklyDesc: 'سيكون تقريرك الأسبوعي جاهزاً بعد ٣ أيام من التسجيل',
        errSubmitFailed: 'تعذّر الحفظ — حاول مرة أخرى',
        errLoadFailed: 'تعذّر تحميل البيانات — حاول مرة أخرى',
        unitKg: 'كجم',
        unitHours: 'ساعة',
        unitMinutes: 'دقيقة',
        unitPercent: '٪',
      },
    ```

    Important constraints:
    - Insert the new `progress` block in all three files BEFORE the final closing brace of the object literal.
    - Add a trailing comma to the new `progress` block in fa.ts (and en.ts and ar.ts) — even if it is the last property, the codebase uses trailing commas.
    - Do NOT modify any existing dictionary keys; insertion is additive.
    - Ensure the `progress` namespace in fa.ts is added to BOTH the `interface Dictionary` block AND the `const fa: Dictionary` object literal.
    - All 49 keys must be present in each of fa/en/ar with exact same key names; TypeScript will fail compilation otherwise.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\s*progress:" frontend/src/dictionaries/fa.ts` returns 2 (one in interface, one in object literal)
    - `grep -c "^\s*progress:" frontend/src/dictionaries/en.ts` returns 1 (just object literal — interface lives in fa.ts)
    - `grep -c "^\s*progress:" frontend/src/dictionaries/ar.ts` returns 1
    - `frontend/src/dictionaries/fa.ts` contains the exact string `title: 'پیشرفت'` and `tabSummary: 'خلاصه'` and `weeklyFocusTitle: 'پیشنهاد هفته بعد'`
    - `frontend/src/dictionaries/en.ts` contains `title: 'Progress'` and `tabSummary: 'Summary'` and `weeklyFocusTitle: 'Focus for next week'`
    - `frontend/src/dictionaries/ar.ts` contains `title: 'التقدم'` and `tabSummary: 'الملخص'`
    - All 8 win labels exist in fa: `winSleep`, `winActivity`, `winLogging`, `winLowStress`, `winLowHunger`, `winHydration`, `winProtein`, `winFiber` (grep all 8)
    - `cd frontend && npx tsc --noEmit` exits 0 (TypeScript enforces all 49 keys exist in all three locales)
  </acceptance_criteria>
  <done>
    Three dictionary files updated with full `progress` namespace. Persian is canonical UI-SPEC copy. TypeScript strict-mode compiles clean.
  </done>
</task>

</tasks>

<verification>
- TypeScript compile passes: `cd frontend && npx tsc --noEmit` → exit 0
- No `any` types: `grep -n ": any" frontend/src/types/progress.ts frontend/src/lib/progress.ts` → no matches
- All 49 progress keys defined in fa.ts interface AND fa/en/ar object literals
- Lib client mirrors nutrition.ts pattern (same auth header helper, same handleResponse with 401 short-circuit)
</verification>

<success_criteria>
- Frontend has typed contracts identical to backend Pydantic schemas (PROG-01/02/03 endpoint shapes mirrored exactly)
- Plan 03 components can `import { submitCheckIn, getProgressSummary, getWeeklyReport } from '@/lib/progress'` and `import type { ... } from '@/types/progress'` with zero ambiguity
- All three dictionary files compile under TypeScript strict mode; missing-key errors are caught at compile time
- Persian copy matches the canonical UI-SPEC Copywriting Contract exactly
</success_criteria>

<output>
After completion, create `.planning/phases/09-progress-reports/09-02-SUMMARY.md` documenting:
- Files created (types/progress.ts, lib/progress.ts)
- Files modified (3 dictionaries, key counts)
- Type contracts exposed for Plan 03
- Any translation choices / deviations
</output>
